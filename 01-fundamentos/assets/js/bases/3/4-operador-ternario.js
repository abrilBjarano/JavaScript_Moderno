// const dia = 6 ;
// const horaActual = 8;

// let horaApertura;
// let mensaje;

// horaApertura = ( [0,6].includes(dia) ) ? 9 : 11;

// // horaActual >= horaApertura ? mensaje = 
// //     `Está abierto` : mensaje = `Está cerrado, hora de apertura ${ horaApertura }`;

// mensaje = ( horaActual >= horaApertura ) ? `Está abierto` : `Está cerrado, hora de apertura ${ horaApertura }`;

// diaActual = ['domingo', 'lunes', 'martes', 'miercoles', 'jueves','viernes', 'sabado',];

// console.log({ horaApertura, mensaje, horaActual,});
// console.log( diaActual[dia] );



const persona = 'eduardo';
let   mensaje;

mensaje = ( persona == 'abril' ) ? `chakalose` : `parrandere`;

console.log( mensaje );
